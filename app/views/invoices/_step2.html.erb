<div class="container">
  <%= turbo_frame_tag "step2_frame" do %>  
    <div class="row">
      <div class="col-md-7">
        <embed src="<%= url_for(@invoice.pdf) %>" width="100%" height="600px" type="application/pdf">
      </div>

      <div class="col-md-5">
        <%= form_with url: connect_booking_path(invoice_id: @invoice.id), local: true, data: { turbo_frame: 'step2_frame' } do |form| %>
          <div class="form-group message">
            <p class="title">Connect the correct booking item ID</p>
            <p class="content">Please lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer tempor eleifend tortor quis vulputate. Integer non felis ac odio imperdiet lobortis id sed erat.</p>
          </div>
          <div class="wrap">
            <p class="title">Connect Booking</p>
            <div class="form-group">
              <%= form.label :booking_item %>
              <%= form.text_field :booking_item, class: "form-control", required: true %>
            </div>
          </div>
          
          <div class="actions next_btn_wrap">
            <%= form.submit 'Go To Next Stop ➜', class: "btn btn-primary mt-3" %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
